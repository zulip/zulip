import MoreVerticalIcon from "~icons/zulip-icon/more-vertical";
import SendIcon from "~icons/zulip-icon/send";

1. Click the **ellipsis** (<MoreVerticalIcon />) next to the
   **send** (<SendIcon />) button.
