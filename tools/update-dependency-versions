#!/usr/bin/env python3
"""Refresh puppet/zulip/files/dependencies.json from upstream release metadata."""

import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), ".."))

from tools.lib.dependency_updater.cli import main

if __name__ == "__main__":
    sys.exit(main())
