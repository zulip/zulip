#!/bin/bash -e

target='zephyr'

case "$1" in
    zephyr*)
      target="$1"
      shift
      ;;
esac

cd "$(dirname "$0")"/..
./manage.py test "$target" --skip-generate --settings=humbug.test_settings "$@"
