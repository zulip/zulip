#!/bin/bash -e

target='zephyr'

case "$1" in
    zephyr*)
      target="$1"
      shift
      ;;
esac

cd "$(dirname "$0")"/..
./zephyr/tests/generate-fixtures
./manage.py test "$target" --settings=humbug.test_settings "$@"
