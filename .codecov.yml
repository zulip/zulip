comment: off

coverage:
  status:
    project:
      default:
        target: auto
        threshold: null
        base: auto
    patch:
      default:
        target: auto
        threshold: null
        base: auto
