{{#if (eq type "user_pill")}}
    {{~!-- squash whitespace --~}}
    {{operator}}
    {{~#each users}}
        {{#if this.valid_user}}
            {{> user_pill .}}
        {{else}}
            {{operand}}
        {{/if}}
    {{~/each~}}
    {{~!-- squash whitespace --~}}
{{~/if~}}

