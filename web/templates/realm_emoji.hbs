<img class="emoji{{#if custom_classes}} {{custom_classes}}{{/if}}" src="{{#if (and still_url (not (eq emoji_animation_setting "always")))}}{{still_url}}{{else}}{{url}}{{/if}}"{{#if (and still_url (eq emoji_animation_setting "on_hover"))}} data-animated-url="{{url}}" data-still-url="{{still_url}}"{{/if}}{{#if alt}} alt="{{alt}}"{{/if}}{{#if tippy_emoji_name}} data-tippy-content=":{{tippy_emoji_name}}:"{{/if}}{{#if lazy}} loading="lazy"{{/if}} />
