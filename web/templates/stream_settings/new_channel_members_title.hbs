<span>
    {{#tr}}
        Add subscribers to <z-channel-name></z-channel-name>
        {{#*inline "z-channel-name"}}
            {{> stream_privacy_icon
              invite_only=sub.invite_only
              is_web_public=sub.is_web_public
              is_archived=sub.is_archived }}
            <span class="stream-name-title">{{sub.name}}</span>
        {{/inline}}
    {{/tr}}
</span>
