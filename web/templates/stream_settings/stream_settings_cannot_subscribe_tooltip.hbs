<template id="cannot-subscribe-tooltip-template">
    <span id="cannot-subscribe-tooltip-content">
        {{#if is_archived}}
            {{#if subscribed}}
                {{#tr}}
                    Can't unsubscribe from archived channel <z-stream></z-stream>
                    {{#*inline "z-stream"}}<span class="white-space-nowrap">{{> ../inline_decorated_stream_name stream=.}}</span>{{/inline}}
                {{/tr}}
            {{else}}
                {{#tr}}
                    Can't subscribe to archived channel <z-stream></z-stream>
                    {{#*inline "z-stream"}}<span class="white-space-nowrap">{{> ../inline_decorated_stream_name stream=.}}</span>{{/inline}}
                {{/tr}}
            {{/if}}
        {{else}}
            {{#tr}}
                Can't subscribe to private channel <z-stream></z-stream>
                {{#*inline "z-stream"}}<span class="white-space-nowrap">{{> ../inline_decorated_stream_name stream=.}}</span>{{/inline}}
            {{/tr}}
        {{/if}}
    </span>
</template>

