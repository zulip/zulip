<div>
    {{#unless is_guest}}
        {{#unless zulip_plan_is_not_limited}}
        {{#if is_business_type_org}}
            <div class="organization-upgrade-banners-container banner-wrapper"></div>
        {{else}}
            <div class="upgrade-or-sponsorship-banners-container banner-wrapper">
            </div>
        {{/if}}
        {{/unless}}
    {{/unless}}
</div>
