<div id="bot-edit-form" data-user-id="{{user_id}}" data-email="{{email}}">
    <form class="edit_bot_form name-setting">
        <div class="alert" id="bot-edit-form-error"></div>
        <div class="input-group name_change_container hideable">
            <label for="edit_bot_full_name" class="modal-field-label">{{t "Name" }}</label>
            <input type="text" autocomplete="off" name="full_name" id="edit_bot_full_name" class="modal_text_input" value="{{ full_name }}" />
        </div>
        <input type="hidden" name="is_full_name" value="true" class="hideable" />
        <div class="input-group email_change_container hideable">
            <label for="email" class="modal-field-label">{{t "Email" }}</label>
            <input type="text" autocomplete="off" name="email" class="modal_text_input" value="{{ email }}" readonly/>
        </div>
        <div class="input-group user_id_container hideable">
            <label for="user_id" class="modal-field-label">{{t "User ID" }}</label>
            <input type="text" autocomplete="off" name="user_id" class="modal_text_input" value="{{ user_id }}" readonly/>
        </div>
        <div class="input-group hideable">
            <label for="bot-role-select" class="modal-field-label">{{t 'Role' }}
                {{> ../help_link_widget link="/help/user-roles" }}
            </label>
            <select name="bot-role-select" id="bot-role-select" class="modal_select bootstrap-focus-style hideable" data-setting-widget-type="number" {{#if disable_role_dropdown}}disabled{{/if}}>
                {{> dropdown_options_widget option_values=user_role_values}}
            </select>
        </div>
        <div class="hideable">
            {{> ../dropdown_widget_with_label
              widget_name="edit_bot_owner"
              label=(t 'Owner')}}
        </div>
        <div id="service_data" class="hideable">
        </div>
        <div class="input-group edit-avatar-section">
            <label for="bot_avatar_file_input" class="modal-field-label">{{t "Avatar" }}</label>
            <input type="file" name="bot_avatar_file_input" class="notvisible edit_bot_avatar_file_input" id="bot_avatar_file_input" value="{{t 'Upload avatar' }}" />
            <div id="edit_bot_avatar_preview_text">
                <img class="edit_bot_avatar_preview_image" id="edit_bot_avatar_preview_image" />
            </div>
            <div id="bot_avatar_file_input_error" class="text-error"></div>
            <div id="image_editor">
                <button type="button" class="modal__btn image_clear btn-small" id="edit_bot_avatar_clear_button">{{t "Clear avatar" }}</button>
                <div>
                    <button type="button" class="modal__btn image_save btn-small" id="edit_bot_avatar_scale_to_fit_button">{{t "Scale to fit" }}</button>
                    <button type="button" class="modal__btn image_save btn-small" id="edit_bot_reset_button">{{t "Reset scale" }}</button>
                    <button type="button" class="modal__btn image_save btn-small" id="edit_bot_avatar_save_button">{{t "Save changes" }}</button>
                </div>
            </div>
            <button type="button" class="button white rounded edit_bot_avatar_upload_button">{{t "Choose avatar" }}</button>
            <div class="inline-block">
                <img class="bot-avatar-image" id="current_bot_avatar_image" src="{{bot_avatar_url}}"/>
            </div>
        </div>
    </form>
    {{#if is_incoming_webhook_bot}}
    <div class="input-group">
        <button class="button rounded generate_url_for_integration">
            {{t 'Generate URL for an integration' }}
        </button>
    </div>
    {{/if}}
    <div class="input-group">
        {{#if is_active}}
        <button class="button rounded button-danger deactivate_bot_button">
            {{t 'Deactivate bot' }}
        </button>
        {{else}}
        <span class="hideable">
            <button id="reactivate-bot" class="button rounded reactivate_user_button hideable">
                {{t 'Reactivate bot' }}
            </button>
        </span>
        {{/if}}
    </div>
</div>
