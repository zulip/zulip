<div id="{{id}}" data-user-count="{{user_count}}" class="buddy-list-subheader hidden-for-spectators">
    <span class="buddy-list-subheader-wrapper">
        <i class="{{toggle_class}} buddy-list-subheader-icon zulip-icon zulip-icon-subheading-triangle-right {{#if is_collapsed}}rotate-icon-right{{else}}rotate-icon-down{{/if}}"
          aria-hidden="true"></i>
        <span class="buddy-list-subheader-name">
            {{header_text}}・{{user_count}}
        </span>
    </span>
</div>
