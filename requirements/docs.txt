# This is used by ReadTheDocs to install dependencies, so it's
# valuable to keep thisfile as just the dependencies for our
# documentation.

# Dependencies of Sphinx
alabaster==0.7.8
babel==2.3.4
CommonMark==0.5.4
docutils==0.12
imagesize==0.7.1
snowballstemmer==1.2.1

# Needed to build docs
recommonmark==0.4.0

# Needed to build RTD docs
sphinx==1.4.1
sphinx-rtd-theme==0.1.9
