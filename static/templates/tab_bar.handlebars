<ul id="tab_list">
{{#each tabs}}
  <li class="{{active}} {{cls}}"  {{#if data}}data-name="{{data}}"{{/if}}>
    {{#if icon}}
      <i class="icon-vector-narrow icon-vector-small"></i>
    {{/if}}
    {{! Most tabs are links, but some are not since we don't have a narrow for them (e.g. Search) }}
    {{#if hash}}
      {{#unless home}}
        <a href="{{hash}}">{{title}}</a>
      {{/unless}}
    {{else}}
      {{#if home}}
        <i class="icon-vector-home"></i>
      {{/if}}
      {{#unless home}}
        <span>{{title}}</span>
      {{/unless}}
    {{/if}}
  </li>
{{/each}}
    <i id="nav_search_trigger" class="icon-vector-search"></i>
</ul>
