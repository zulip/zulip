{{#with stream}}
<tr class="stream_row">
  <td>
      <span class="stream_name">{{name}}</span>
  </td>
  <td>
      <button class="btn deactivate btn-danger">
        Unsubscribe everyone
      </button>
  </td>
</tr>
{{/with}}
