<div id="muted-user-settings" class="settings-section" data-name="muted-users">
    <table id="muted_users_table" class="table">
        <thead>
            <th>{{t "Users" }}</th>
            <th class="actions">{{t "Actions" }}</th>
        </thead>
        <tbody class="required-text" data-empty="{{t 'You do not have any muted users yet.'}}"></tbody>
    </table>
</div>
