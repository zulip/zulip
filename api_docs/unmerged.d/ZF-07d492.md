**Feature level ZF-07d492**

* [`DELETE /messages`](/api/delete-messages): Added a new endpoint to
  permanently delete multiple messages in a single request, given a list
  of message IDs. Deletion is all-or-nothing, and uses the same permission
  and content-access rules as [`DELETE
  /messages/{message_id}`](/api/delete-message).
