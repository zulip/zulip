* [`PATCH /settings`](/api/update-settings),
  [`PATCH /realm/user_settings_defaults`](/api/update-realm-user-settings-defaults):
  Added `web_link_previews_enabled` boolean parameter, which controls
  whether link previews are shown by default in the web/desktop
  application. Defaults to `true`.
