* [`GET /events`](/api/get-events), [`GET /realm/linkifiers`](/api/get-linkifiers),
  [`POST /realm/filters`](/api/add-linkifier),
  [`PATCH /realm/filters/{filter_id}`](/api/update-linkifier):
  Added `example_input` to linkifier objects to provide a sample string matching the pattern.
  This is required for reverse linkifiers. Pass an empty string during PATCH to set this
  field back to null.
