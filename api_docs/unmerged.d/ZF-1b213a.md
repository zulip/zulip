* [`POST /register`](/api/register-queue), [`GET /events`](/api/get-events),
  `PATCH /realm`: Galène integration added as an option for the realm setting
  `video_chat_provider`.
* [`POST /calls/galene/create`](/api/create-galene-video-call): Added a new
  endpoint to create a Galène video call URL.
