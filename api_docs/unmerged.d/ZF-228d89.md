* `PATCH /bots/{bot_user_id}`: Added `bot_type` parameter to support
  changing a bot's type via this endpoint.
* `PATCH /bots/{bot_user_id}`: Added the `embedded_bot_service_name` parameter to support changing an embedded bot's service name.
