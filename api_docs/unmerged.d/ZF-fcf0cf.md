**Feature level ZF-fcf0cf**

* [`POST /messages`](/api/send-message): Added `message_url` and
  `message_link` fields to the response, containing the URL of the sent
  message and a Markdown link to it, respectively.
