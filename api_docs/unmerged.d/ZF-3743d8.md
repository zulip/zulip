* `PATCH /bots/{bot_user_id}`: Added `bot_type` parameter to support
  changing a bot's type.
* `PATCH /bots/{bot_user_id}`: Added the `service_name` parameter to support changing an embedded bot's service name.
