* [`PATCH /invites/multiuse/{invite_id}`](/api/edit-invite-link):
  Added support for editing `invite_as`, `group_ids` and `stream_ids` fields
  and sending `welcome_message_custom_text` in reusable invitation links.
