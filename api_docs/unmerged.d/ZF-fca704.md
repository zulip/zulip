* `PATCH /realm`, [`POST /register`](/api/register-queue),
  [`GET /events`](/api/get-events): Replaced the
  `name_changes_disabled` boolean setting and
  `realm_name_changes_disabled` field with the
  `can_change_own_name_group` and `realm_can_change_own_name_group`
  [group-setting values](/api/group-setting-values).
