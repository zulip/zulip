* `PATCH /realm`, [`POST /register`](/api/register-queue),
  [`GET /events`](/api/get-events): Replaced the
  `name_changes_disabled` boolean setting with
  `can_change_own_name_group`, a
  [group-setting value](/api/group-setting-values) defining which users
  can change their own names.
