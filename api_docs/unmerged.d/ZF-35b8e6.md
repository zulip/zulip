* [`GET /events`](/api/get-events): `giphy_rating` is now used to denote
  the common rating configured for both Tenor and GIPHY integrations.
* [`POST /register`](/api/register-queue): Added new `tenor_api_key`
  and `tenor_api_client` fields, which are required to fetch GIFs
  using the Tenor API. Renamed `giphy_rating_options` to `gif_rating_options`
  to generalize the ratings for both GIPHY and Tenor integrations.
  `realm_giphy_rating` is now used to denote the common rating configured
  for both Tenor and GIPHY integrations.
