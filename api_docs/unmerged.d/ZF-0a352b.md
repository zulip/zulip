* `PATCH /realm`, [`POST /register`](/api/register-queue),
  [`GET /events`](/api/get-events): Added a new
  `image_thumbnail_size` realm setting that controls the size of
  image and video thumbnails in messages.
