**Feature level ZF-c876d2**

* [`GET /calls/jitsi/create`](/api/create-jitsi-video-call): Added new
  endpoint that generates a Jitsi Meet video call URL with JWT authentication.

* [`POST /register`](/api/register-queue), [`GET /events`](/api/get-events):
  Added `server_jitsi_app_id` field to the server settings data. This
  field contains the Jitsi app ID when the server is configured for JWT
  authentication, or `null` otherwise.
