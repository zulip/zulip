* [`GET /calls/jitsi/create`](/api/create-jitsi-video-call): Added a new
  endpoint to generate a Jitsi Meet video call URL with JWT
  authentication.

* [`POST /register`](/api/register-queue): Added
  `server_jitsi_jwt_configured` boolean field to the response,
  indicating whether the Zulip server has JWT credentials set for its
  configured Jitsi Meet server. Combined with `realm_jitsi_server_url`,
  clients use this to decide whether to invoke the JWT-signed call
  flow.

* [`POST /register`](/api/register-queue): The client contract for
  `realm_jitsi_server_url` and `server_jitsi_server_url` was extended:
  when `server_jitsi_jwt_configured` is `true` and
  `realm_jitsi_server_url` is `null`, clients must call
  [`GET /calls/jitsi/create`](/api/create-jitsi-video-call) to obtain a
  call URL rather than constructing one from these fields directly.
