* [`GET /streams/{stream_id}`](/api/get-stream-by-id): Added a new field
  `subscriber_count` to stream objects, which counts the total number of
  non-deactivated users who are subscribed to the stream.
