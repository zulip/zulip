**Feature level ZF-a43758**

* [`POST /calls/livekit/create`](/api/create-livekit-video-call):
  Added new endpoint to create LiveKit video call URLs.

* `POST /register`, `GET /events`, `PATCH /realm`: Integrated Zulip
  calling (LiveKit) added as an option for the realm setting
  `video_chat_provider`.
