**Feature level ZF-f5d3e9**

* `PATCH /realm`, [`POST /register`](/api/register-queue),
  [`GET /events`](/api/get-events): Added a new `enable_mcp_access`
  realm setting, controlling whether AI agents may access the
  organization through the Zulip server's native MCP endpoint.
