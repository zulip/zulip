* `PATCH /realm`, [`POST /register`](/api/register-queue),
  [`GET /events`](/api/get-events): Added a new `enable_mcp_read_access`
  realm setting, controlling whether users may connect AI agents that
  read the organization through the Zulip server's native MCP endpoint.
