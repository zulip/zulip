* [`POST /register`](/api/register-queue): The `unread_msgs` object now includes
  a `watched_phrases` array, parallel to the `mentions` array.
* [`GET /events`](/api/get-events): The `message_details` object in
  `update_message_flags` events now includes a `has_watched_phrase` boolean
  field, alongside the existing `mentioned` field.
