* [ GET /users/me/api_keys ](/api/get-api-keys):
  Retrieve all API keys belonging to the currently
  authenticated user.
* [ DELETE /users/me/api_keys/{key_id} ](/api/revoke-api-key):
  Revoke a specific API key by its ID for the
  currently authenticated user.
