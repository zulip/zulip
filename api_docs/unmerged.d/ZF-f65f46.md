* [`GET /events`](/api/get-events): Added `rendered_description` field to the
  realm update event when the `description` property is changed. This field
  contains the HTML-rendered version of the organization description.
