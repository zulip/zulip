* **Channel events in private streams**: When the `send_channel_events_messages`
  realm setting is enabled, subscription and unsubscription events in
  private streams now generate system messages in the `channel events` topic.
