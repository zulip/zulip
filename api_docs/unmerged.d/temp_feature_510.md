* [`POST /register`](/api/register-queue), [`GET /events`](/api/get-events):
  Added `can_change_user_emails` field to the current user's data, and
  `server_admins_can_change_user_emails` field to realm data, indicating
  whether the server allows organization administrators to change user
  email addresses.
