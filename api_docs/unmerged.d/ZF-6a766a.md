* [`POST /messages`](/api/send-message): Added support for the
  [`Idempotency-Key`](/api/http-headers#the-idempotency-key-request-header)
  request header. When a client includes this optional header, the server
  guarantees that the message is sent at most once, even if the same
  request is retried or replayed with the same key.
