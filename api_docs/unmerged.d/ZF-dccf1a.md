* [`GET /events`](/api/get-events): `realm_emoji/update` event has
  been removed, and replaced with `realm_emoji/add` and
  `realm_emoji/remove` events, which only send the added or removed
  emoji, and not all of the emoji.
