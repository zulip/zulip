* [`POST /register`](/api/register-queue), [`GET /events`](/api/get-events),
  [`POST /realm/profile_fields`](/api/create-custom-profile-field),
  [`GET /realm/profile_fields`](/api/get-custom-profile-fields) Added a new
  parameter `use_for_user_matching` to custom profile field objects, which indicates whether
  this custom profile field should be used to match users in @ mention suggestions.
