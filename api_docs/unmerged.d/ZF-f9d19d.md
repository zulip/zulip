* [`POST /register`](/api/register-queue): Renamed the `config_options`
  field in `realm_incoming_webhook_bots` to `url_options`. The list of
  `WebhookConfigOption` in `url_options` is now only used for integration
  URL parameter options.
