* [`GET /users`](/api/get-users), [`GET /users/{user_id}`](/api/get-user),
  [`GET /users/{email}`](/api/get-user-by-email): Fixed a bug which was introduced
  in feature level 232 where guest users might incorrectly receive fake
  backwards-compatibility users of the format used for clients without the
  `user_list_incomplete` client capability on these endpoints.
