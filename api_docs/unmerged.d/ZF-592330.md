* [`POST /register`](/api/register-queue): Added the
  `realm_upload_quota_used_bytes` field to the response, containing the
  total number of bytes currently used for uploaded files in the
  organization. Present if `realm` is among the requested
  `fetch_event_types`, except for unauthenticated spectator clients.
* [`GET /events`](/api/get-events): The `realm` op `update_dict` event
  for the `default` property can now contain an `upload_quota_used_bytes`
  field, sent to everyone in the organization when a file is uploaded or
  deleted. Previously, the organization's total upload usage was only
  available to the owner of the file, via the `upload_space_used` field
  of the `attachment` event.
