**Feature level ZF-546a5d**

* [All REST API endpoints](/api/http-headers#rate-limiting-response-headers):
  The `retry-after` value in rate limit error responses is now rounded up
  to the nearest hundredth of a second. Previously, it was an unrounded
  number of seconds, which could describe a wait shorter than the
  client's round trip to the server.
