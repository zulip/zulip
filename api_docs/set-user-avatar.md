{generate_api_header(/users/{user_id}/avatar:post)}

## Usage examples

{start_tabs}

{generate_code_example(python)|/users/{user_id}/avatar:post|example}

{tab|curl}

{generate_code_example(curl)|/users/{user_id}/avatar:post|example}

{end_tabs}

## Parameters

{generate_api_arguments_table|zulip.yaml|/users/{user_id}/avatar:post}

{generate_parameter_description(/users/{user_id}/avatar:post)}

## Response

{generate_return_values_table|zulip.yaml|/users/{user_id}/avatar:post}

{generate_response_description(/users/{user_id}/avatar:post)}

#### Example response(s)

{generate_code_example|/users/{user_id}/avatar:post|fixture}
